# ===========================================================================
# Agent 平台 —— Python 依赖
#
# 安装： pip install -r requirements.txt
#
# 版本策略说明：
#   所有依赖只约束大版本区间（>=x,<y），让 pip 自行求解出一组互相兼容的组合。
#   尤其是 chromadb / langchain-chroma / langchain-openai 三者存在隐式版本约束，
#   手动钉死 chromadb 版本极易导致 langchain-chroma 导入失败。
#   实测通过的具体版本记录见 README「依赖版本」一节。
# ===========================================================================

# ---------------------- LangChain 0.3.x 家族 ----------------------
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0
langchain-chroma>=0.1.4,<0.3.0
langchain-openai>=0.2.0,<0.4.0

# ---------------------- 向量库 ----------------------
chromadb>=0.5.0,<1.1.0

# ---------------------- Embedding / Rerank ----------------------
sentence-transformers>=3.0.0,<6.0.0

# ---------------------- 稀疏检索（BM25 + 中文分词） ----------------------
rank-bm25>=0.2.2,<0.3.0
jieba>=0.42.1,<0.43.0

# ---------------------- Web 框架 ----------------------
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
python-multipart>=0.0.9          # FastAPI 接收文件上传必需
gradio>=4.44.0,<6.0.0
httpx>=0.27.0,<0.29.0            # ui.py 独立启动时调用 FastAPI

# ---------------------- 数据校验与配置 ----------------------
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# ---------------------- 文档解析 ----------------------
pypdf>=4.0.0,<7.0.0
python-docx>=1.1.0,<2.0.0
docx2txt>=0.8                    # Docx2txtLoader 依赖此独立包
unstructured>=0.15.0             # Docx2txtLoader 的底层依赖
# 仅用于生成中文样例 PDF（scripts/make_sample_docs.py）；不参与平台运行
reportlab>=4.0.0,<6.0.0

# ---------------------- 测试 ----------------------
pytest>=8.0.0,<10.0.0
